import React from 'react'
import {
  Button,
} from 'react-bootstrap'

const PlusBrain = ({ onPlusClick }) => (
  <Button>
    + BRAIN 
  </Button>
)

PlusBrain.propTypes = {
}

export default PlusBrain
