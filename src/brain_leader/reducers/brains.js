const brain = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

const brains = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default brains
